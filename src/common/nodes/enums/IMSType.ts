/**
 * enum with all (currently not) supported issue management systems
 */
 export enum IMSType {
    GITHUB = "GITHUB",
    GITLAB = "GITLAB",
    JIRA = "JIRA",
    REDMINE = "REDMINE",
    CCIMS = "CCIMS"
}