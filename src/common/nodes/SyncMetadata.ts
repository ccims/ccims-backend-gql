/**
 * metadata for the sync
 * contains the id of the ImsSystenm, but the rest in unspecified
 */
export interface SyncMetadata {
    /**
     * the id of the ImsSystem which is associated with this metadata
     */
    id: string
}