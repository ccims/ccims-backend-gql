import * as postgres from "../config/postgres.json";
import * as api from "../config/api.json";
import * as common from "../config/common.json";
export let config = {
    postgres: postgres,
    api: api,
    common: common
}